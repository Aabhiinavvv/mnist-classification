# mnist-classification
This project implements a neural network model to classify handwritten digits (0–9) using the MNIST dataset. It covers data preprocessing, model training, evaluation, and prediction using a deep learning approach.
